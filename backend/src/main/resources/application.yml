server:
  port: 8080


spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/zhilulinghang?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ...
    password: ...
  application:
    name: backend

mybatis:
  configuration:
    map-underscore-to-camel-case: true

deepseek:
  base-url: https://api.deepseek.com
  api-key: ${DEEPSEEK_API_KEY:}
  model: deepseek-v4-flash
  timeout-seconds: 30
